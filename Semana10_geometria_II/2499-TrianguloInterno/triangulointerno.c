#include <stdio.h>

int main(void)
{
	int S, N, M;
	int C1, C2, C3;
	
	while( 1 )
	{
		scanf("%d%d%d", &S, &N, &M);
		if( S==0 && N==0 && M==0)
			break;
		scanf("%d%d%d", &C1, &C2, &C3);
		
		
	}
	return 0;
}
